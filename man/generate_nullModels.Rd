% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MaxEnt_nullModel.R
\name{generate_nullModels}
\alias{generate_nullModels}
\title{Generate Null Models for Species Distribution Model Evaluation}
\usage{
generate_nullModels(
  output.dir,
  SDM.obj,
  model,
  taxon.name,
  iter,
  null.iter = 100,
  parallel = FALSE,
  numCores = 4
)
}
\arguments{
\item{output.dir}{Character string specifying path to output directory}

\item{SDM.obj}{ENMeval object containing model results}

\item{model}{Character string specifying the name of the best model settings within `SDM.obj`, as selected by `best_model()`}

\item{taxon.name}{Character string specifying the species/taxon name, used for output file naming and messages}

\item{iter}{Character or numeric value used to distinguish this null model run in the output filename (e.g. species iteration index)}

\item{null.iter}{Numeric value specifying number of null iterations (default 100)}

\item{parallel}{Logical indicating whether to use parallel processing (default FALSE)}

\item{numCores}{Numeric value specifying number of cores for parallel processing (default 4)}
}
\value{
ENMnulls object containing null model results
}
\description{
Creates null models to assess significance of SDM predictions by randomizing occurrence data
}
\details{
Uses best model parameters (feature classes and regularization multiplier) to generate
null models by randomly sampling points from the background area. The null distribution
can be used to assess statistical significance of the actual model.
}
\examples{
\dontrun{
nulls <- generate_nullModels("output/nulls/", sdm_result, null.iter = 100)
}
}
