% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MaxEnt_pipelines.R
\name{maxent_futurePrediction_pipe}
\alias{maxent_futurePrediction_pipe}
\title{Maxent Future Prediction Pipeline}
\usage{
maxent_futurePrediction_pipe(
  env.vars.future,
  ensemble.climates = T,
  thresh.method = "maxsss",
  data.dir = "data/maxent/",
  output.dir = "outputs/maxent/",
  ensemble.dir = "data/ensembleFutureEnv"
)
}
\arguments{
\item{env.vars.future}{SpatRaster or nested list of SpatRasters (time.period > ssp, or time.period > ssp > gcm) of future environmental variables}

\item{ensemble.climates}{Logical, whether future climate rasters have been ensembled across GCMs (default TRUE)}

\item{thresh.method}{Character, thresholding method passed to `write_model_threshold()` (default "maxsss")}

\item{data.dir}{Character, path to data directory containing saved model outputs}

\item{output.dir}{Character, path to output directory}

\item{ensemble.dir}{Character, path to directory containing ensembled climate rasters (default "data/ensembleFutureEnv")}
}
\value{
None. Writes future prediction and threshold rasters, and stacked prediction rasters, to disk
}
\description{
Pipeline for projecting saved MaxEnt models onto future climate scenarios and writing thresholded prediction rasters to disk
}
\details{
Loads saved SDM outputs and model selection choices, then for each taxon projects the selected model (via `predict_maxnet()` or `predict_maxentJar()`) onto the supplied future environment(s), writing individual and stacked prediction and threshold rasters to disk. Handles a single SpatRaster, a time > ssp ensemble list, or a time > ssp > gcm list.
}
