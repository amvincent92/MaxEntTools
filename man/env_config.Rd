% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_config.R
\name{env_config}
\alias{env_config}
\title{Configure MaxEnt Environment Settings (staging replacement)}
\usage{
env_config(algorithm, seed = 343, java.mem.gb = 8, force.reset = FALSE)
}
\arguments{
\item{algorithm}{Character string specifying the MaxEnt algorithm to configure for, either "maxent.jar" or "maxnet". Only "maxent.jar" requires the Java environment reset}

\item{seed}{Numeric value for random seed to ensure reproducibility}

\item{java.mem.gb}{Numeric value specifying Java memory allocation in GB (default 8)}

\item{force.reset}{Logical. If `TRUE`, forces the detach/reload sequence to run again even if the JVM has already been initialised this session. This is unsafe once a JVM is live and should only be used if you have independently confirmed rJava is not currently attached (e.g. after manually detaching it yourself). Default `FALSE`.}
}
\value{
No return value, called for side effects
}
\description{
Sets up the R environment for running MaxEnt models by configuring Java memory and loading required packages
}
\details{
This function:
- Loads required namespaces (without attaching them to the search path)
- Configures Java memory settings, once per session
- Sets random seed for reproducibility
}
\note{
Java must be installed locally to run MaxEnt. See java_setup.R for more details

This is a staging replacement for the original `env_config()` in
MaxEnt_modelGeneration.R. Unlike the original, it uses `requireNamespace()`/`unloadNamespace()`
instead of `library()`/`detach()`, since all downstream calls to rJava, dismo and ENMeval in
this package are already namespace-qualified (`dismo::`, `ENMeval::`) and do not require these
packages to be attached to the search path. A session-level guard (`.java.state`) prevents the
unload/reload sequence from running more than once per session, because rJava's embedded JVM
cannot be safely reinitialised once started.

Caveat: `.java.state` is defined at package top-level and is re-created (reset to
uninitialised) whenever the package namespace is reloaded, e.g. via `devtools::load_all()`
during development. If the JVM was already started in a previous `load_all()` call within the
same R session, this guard will not detect that and a further `load_all()` followed by
`algorithm = "maxent.jar"` may still crash. This caveat does not apply to normal use of an
installed/attached package, only to iterative package development.
}
